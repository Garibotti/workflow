import { DatetimeTransparentDirective } from './datetime-transparent.directive';

describe('DatetimeTransparentDirective', () => {
  it('should create an instance', () => {
    const directive = new DatetimeTransparentDirective();
    expect(directive).toBeTruthy();
  });
});
